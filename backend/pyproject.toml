[project]
name = "coding-agent-monitor-backend"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "watchdog>=3.0.0",
]
